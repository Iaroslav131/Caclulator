Simple calculator application built with Python, PySide6, and Qt Designer.

Run `main.py` to start the application.

Includes a Qt Designer UI file (`design.ui`), generated UI code (`design.py`), Qt resources (`files.qrc`, `files_rc.py`), and custom icons.

Requirements:
pip install PySide6
